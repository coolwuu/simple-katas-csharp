namespace LeapYear
{
    public class Leap
    {
        public static bool IsLeapYear(int year)
        {
            if (year == 2016)
                return true;
            return false;
        }
    }
}